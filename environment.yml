name: auto-clicker
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - pyautogui==0.9.54
      - Pillow==10.4.0
      - pygetwindow==0.0.9  # Windows/Linux only; not used on macOS
      - playwright>=1.44.0
